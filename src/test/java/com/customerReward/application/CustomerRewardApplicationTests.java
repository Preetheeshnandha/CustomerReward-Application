package com.customerReward.application;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Test class for the {@link CustomerRewardApplication}.
 *
 * <p>This test ensures that the Spring application context loads successfully
 * without throwing any exceptions. It acts as a basic sanity check
 * for application configuration and bean initialization.</p>
 *
 * <p>Annotated with {@code @SpringBootTest} to load the full application context.</p>
 * 
 * <p>Typically used as a default test to verify that the application can start.</p>
 * 
 * @author Preetheeshnandha
 */
@SpringBootTest
class CustomerRewardApplicationTests {

	@Test
	void contextLoads() {
	}

}
